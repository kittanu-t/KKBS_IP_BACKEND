# 🧠 Obesity Prediction Models

โปรเจคนี้เป็นการพัฒนาโมเดล Machine Learning สำหรับทำนายภาวะโรคอ้วน (Obesity Prediction) โดยมีการเปรียบเทียบโมเดลหลายแบบเพื่อดูประสิทธิภาพและจำนวน feature ที่ใช้

## 📂 Project Structure
 ``` 
├── knn_model/          # โมเดล KNN ที่ปรับจูนด้วย Optuna
├── knn_minimal_model/  # โมเดล KNN ที่ปรับจูนด้วย Optuna + Wrapper Method
├── obesity_model/      # โมเดล Random Forest + PCA
├── app.py              # แอปทำนายผลด้วย KNN model + Wrapper Method
├── app_knn.py          # แอปทำนายผลด้วย KNN model
├── app_mix.py          # แอปทำนายผลด้วย RF + PCA model
└── requirements.txt    # dependencies
 ``` 

## 🔍 Models Overview
✅ app.py
* ใช้โมเดล: K-Nearest Neighbors (KNN) + Wrapper Method
* ปรับจูนพารามิเตอร์ด้วย Optuna และใช้ เทคนิค Wrapper เพื่อลด Features 
* โหลดโมเดลจาก: knn_minimal_model/
* ใช้จำนวน น้อย 6 features
* Accuracy ≈ 0.90
* เหมาะสำหรับ: ความแม่นยำสูง

app_knn.py
* ใช้โมเดล: K-Nearest Neighbors (KNN)
* ปรับจูนพารามิเตอร์ด้วย Optuna
* โหลดโมเดลจาก: knn_model/
* ใช้จำนวน features มากกว่า
* Accuracy ≈ 0.95
* เหมาะสำหรับ: ความแม่นยำสูงสุด

## ⚖️ app_mix.py
* ใช้โมเดล: Random Forest + PCA
* โหลดโมเดลจาก: obesity_model/
* ลดจำนวน features ด้วย PCA
* Accuracy ≈ 0.78
* เหมาะสำหรับ: โมเดลที่เบาและใช้ feature น้อย

## 🚀 Installation & Usage
### 1️⃣ สร้าง Virtual Environment
``` bash
py -m venv .venv
```

### 2️⃣ เปิดใช้งาน Environment
Windows
```
.venv\Scripts\activate
```

Mac/Linux
```
source .venv/bin/activate
```

### 3️⃣ ติดตั้ง Dependencies
```
pip install -r requirements.txt
```

### 4️⃣ รันแอป (KNN Model)
```
py app_knn.py
```

(Optional) รันโมเดลแบบ RF + PCA
```
py app_mix.py
```

## 📊 Summary
Model	| Features | Accuracy |	จุดเด่น
---| ---| ---| ---|
Optuna + Warpper |	น้อย	| ~0.90 |	แม่นยำสูง + feature น้อย
KNN + Optuna |	มาก	| ~0.95 |	แม่นยำสูง
RF + PCA |	น้อย	| ~0.78 |	เบาและเร็ว

## 🧑‍💻 Notes
* โมเดลถูกเทรนและบันทึกไว้ในโฟลเดอร์ model
* KNN เหมาะกับงานที่ต้องการความแม่นยำ
* RF + PCA เหมาะกับงานที่ต้องการความเร็วและลดมิติข้อมูล
